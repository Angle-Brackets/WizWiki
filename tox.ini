[tox]
envlist = py39, py310, py311, py312, flake8
isolated_build = True

[testenv]
deps =
    pytest
    pytest-asyncio
    pydantic
    cloudscraper
    beautifulsoup4
commands =
    pytest {posargs:tests}

[testenv:flake8]
deps = flake8
commands = flake8 src tests
